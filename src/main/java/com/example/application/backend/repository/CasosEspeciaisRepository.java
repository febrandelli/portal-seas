package com.example.application.backend.repository;

import com.example.application.backend.domain.CasosEspeciais;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CasosEspeciaisRepository extends JpaRepository<CasosEspeciais, Integer> {
}
